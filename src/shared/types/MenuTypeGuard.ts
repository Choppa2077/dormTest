// import { MenuItemType, SubMenuType } from 'antd/es/menu/interface';

// export function isMenuItemType(item: any): item is MenuItemType {
//   return item && 'label' in item;
// }

// export function hasOnClick(item: any): item is { onClick: (e: any) => void } {
//   return item && 'onClick' in item && typeof item.onClick === 'function';
// }
